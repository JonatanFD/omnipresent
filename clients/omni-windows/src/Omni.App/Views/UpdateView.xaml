<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Omni.App.Views.UpdateView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="18" MaxWidth="640">

            <!-- Version info -->
            <StackPanel Spacing="8" Visibility="{x:Bind ViewModel.DaemonVersion, Mode=OneWay, Converter={StaticResource EmptyStringToVisibilityConverter}}">
                <TextBlock Text="Version" Style="{StaticResource SubtitleTextBlockStyle}" />
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="8" Padding="12">
                    <TextBlock Text="{x:Bind ViewModel.DaemonVersion, Mode=OneWay}" />
                </Border>
            </StackPanel>

            <!-- Update button -->
            <StackPanel Spacing="8">
                <TextBlock Text="Updates" Style="{StaticResource SubtitleTextBlockStyle}" />
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="8" Padding="12">
                    <StackPanel Spacing="12">
                        <Button x:Name="UpdateButton" Click="OnUpdateClick">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <ProgressRing x:Name="UpdateProgress" IsActive="False" Width="20" Height="20" />
                                <TextBlock Text="Update Omnipresent" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <TextBlock x:Name="UpdateMessage" Text=""
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   TextWrapping="Wrap"
                                   Visibility="Collapsed" />
                        <TextBlock Text="Downloads and installs the latest release, then restarts the daemon."
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Style="{StaticResource CaptionTextBlockStyle}" />
                    </StackPanel>
                </Border>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
