//! The `omni` binary: the single entry point for all user interaction.
//!
//! Parses commands (`start`, `stop`, `status`, `connect`, `accept`, ...) and
//! talks to the Runtime daemon over local IPC. Commands and wiring are added as
//! the modules behind them are implemented.

fn main() {
    println!("omni: not yet implemented");
}
